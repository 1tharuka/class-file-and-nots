// ternary operator  = shrtcut for an if/ else statement
//                      Takes 3 operands
                        //  1 a condition with
                        // expression if true
                        // expression if false
// condition expriftrue : exprifalse

let age = function1(10);
function function1(age){
    return age >= 18 ? true : false
};
console.log(age);


function geames(wins) {
    wins ? console.log('You wins') : console.log("You Lose");
}

geames(true);