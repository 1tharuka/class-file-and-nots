//  callbacks = a function passed as an argument
//              to another funtion

let totle = sum(2, 3);
displaConsole(totle)
function sum(x, y) {
 let totle = x + y;  
 return totle;
}

function displaConsole(utbute) {
    console.log(utbute);
}